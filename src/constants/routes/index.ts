export enum RoutePaths {
  InitialPage = '/',
  AuthPage = '/auth',
  // MainPages = '/main',
  DashboardPage = '/dashboard',
  AccountPage = '/account',
  SettingsPage = '/settings',
  CallUserPage = '/callUser/:userName',
}
